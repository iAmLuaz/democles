module.exports = async function (context, req) {
  const apiUrl = process.env.API_URL

  context.res = {
    status: 200,
    headers: {
      'Content-Type': 'application/json',
      'Access-Control-Allow-Origin': '*'
    },
    body: {
      ok: true,
      apiUrl,
      note: 'Esta es la URL EXACTA traída desde Key Vault / Application Settings'
    }
  }
}
